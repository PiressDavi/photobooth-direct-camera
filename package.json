{
  "name": "photobooth-direct-camera",
  "version": "1.0.0",
  "scripts": {
    "build-client": "cd client && npm install && npm run build",
    "start": "cd server && npm install && node index.js",
    "start-all": "npm run build-client && npm run start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
